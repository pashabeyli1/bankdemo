server:
  port: 8082
  servlet:
    context-path: /bank


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bankdemodb
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
